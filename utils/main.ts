export const Hoge = (str: string) => {
  return str + '-hoge';
};

export { ObjectCopy, ObjectKeys, ObjectValues } from '@/server/utils/object';

export { IntNullable } from '@/server/utils/convert';
